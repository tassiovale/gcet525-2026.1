main {
int total = 0;
int count = 0;
int value;
bool done = false;

while (!done) {
read(value);

if (value < 0) {
done = true;
} else {
total = total + value;
count = count + 1;
}

if (done) {
print(true, total, count);
} else {
print(false, total, count);
}
}
}